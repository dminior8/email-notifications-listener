server:
  port: 8081

spring:
  application:
    name: email-notifications-listener
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest
  data:
    mongodb:
      uri: mongodb://root:example@localhost:27017/notifications-db?authSource=admin


